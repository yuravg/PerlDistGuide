#!/usr/bin/env perl

use strict;
use warnings;
use FindBin qw($Bin);
use lib "$Bin/../lib";
use Example::CLI;

# VERSION   <- Allows Dist::Zilla to inject the version

# Let the CLI module handle everything
exit Example::CLI->new()->run(@ARGV);
